<script lang="ts">
	import './PiLlamaInstallationCommands.css';

	const PI_LOGO =
		'https://huggingface.co/buckets/julien-c/my-training-bucket/resolve/pi-logo-dark.svg';
</script>

<div
	class="pi-llama-installation-commands bg-foreground/4 border-secondary relative w-full overflow-hidden rounded-xl border"
>
	<div class="overflow-auto text-[14px]">
```bash
# 1. Serve a model
llama serve

# 2. Install the pi-llama plugin
pi install git:github.com/huggingface/pi-llama

# 3. Run Pi, everything is set
pi
```
	</div>

	<div
		class="dark:bg-muted-foreground absolute top-4 right-4 flex size-[2.4rem] items-center justify-center rounded-md bg-white p-[0.4rem] shadow-sm"
	>
		<img src={PI_LOGO} alt="Pi" class="h-full w-auto" />
	</div>
</div>
