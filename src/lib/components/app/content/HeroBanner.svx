<script lang="ts">
	import InstallCommand from './InstallCommand.svelte';
</script>

<section class="flex flex-col items-center gap-16 py-24 md:py-48">
	<div class="flex flex-col items-center gap-6 text-center">

        # AI that lives on your computer.<br class="hidden md:inline" /> Open-source, private &amp; always local.

		Run frontier AI entirely on your machine. No API keys, no telemetry, no limits. Own your
		models and conversation data.
	</div>

	<InstallCommand />
</section>

<style>
    @reference "tailwindcss";

    section :global(h1) {
        @apply text-2xl leading-tight font-semibold sm:text-4xl;
    }

    section :global(p) {
        @apply text-lg leading-relaxed max-w-xl;
        color: var(--muted-foreground);
    }
</style>
