<script lang="ts">
    import ShowCaseSection from '$lib/components/app/content/ShowcaseSections/ShowcaseSection.svelte';
	import PiLlamaInstallationCommands from '$lib/components/app/content/PiLlamaInstallationCommands/PiLlamaInstallationCommands.svx';
    import HardwareReels from '$lib/components/app/content/HardwareReels.svelte';
</script>

<div class="space-y-12 py-12 md:py-24">
    <ShowCaseSection>
        ## Pair it with a local coding agent.
       	Run `llama serve`, install the `pi-llama` plugin and launch [Pi](https://github.com/earendil-works/pi). It will automatically discover your local model. No config, no API keys. Files stay on your machine, requests never leave it.

        {#snippet mediaContent()}
		    <PiLlamaInstallationCommands />
		{/snippet}
    </ShowCaseSection>

	<ShowCaseSection>
        ## Optimized for any hardware.
       	From your laptop to a cluster, llama.cpp runs on whatever you have. Same binary, same models, same hand-tuned kernels for every GPU and CPU.

        {#snippet mediaContent()}
            <HardwareReels />
        {/snippet}
	</ShowCaseSection>
</div>
