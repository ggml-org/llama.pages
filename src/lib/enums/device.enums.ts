export enum MacDeviceType {
	MACBOOK = 'macbook',
	DESKTOP = 'desktop',
	UNKNOWN = 'unknown'
}

export enum OsKind {
	MAC = 'mac',
	WINDOWS = 'windows',
	LINUX = 'linux',
	IOS = 'ios',
	ANDROID = 'android',
	UNKNOWN = 'unknown'
}

export enum DeviceFormFactor {
	DESKTOP = 'desktop',
	MOBILE = 'mobile',
	TABLET = 'tablet'
}
