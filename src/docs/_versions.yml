# Ordered list of docs versions. `main` is built from the working tree;
# every other entry must be a git tag of this repo — its docs are extracted
# from `<tag>:src/docs/main` at build time by scripts/extract-docs.js.
- main
